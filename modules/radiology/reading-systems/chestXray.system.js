// server/modules/radiology/reading-systems/chestXray.system.js
//
// Система чтения рентгена органов грудной клетки (CXR) — пилотная
// модальность MVP. Выбрана намеренно: один-два кадра без стека срезов,
// поэтому проверяет модель взаимодействия и оценки с минимумом возни с
// листанием — а движок при этом уже общий и готов принять КТ/УЗИ.
//
// Чек-лист — классический системный осмотр снимка грудной клетки
// (мнемоника ABCDE + мягкие ткани). Порядок пунктов — это и есть «читать
// правильно»: не прыгать к очевидному затемнению, а пройти всё.

import { defineReadingSystem } from "./base.js";

export default defineReadingSystem({
  modality: "cxr",
  title: "Рентген органов грудной клетки",
  description:
    "Обзорная рентгенограмма ОГК: систематический осмотр по ABCDE, локализация находок и заключение.",
  viewer: {
    multiSlice: false,
    tools: ["zoom", "pan", "measure", "roi"],
    // Классической «оконной» настройки у обычного рентгена нет — снимок
    // приходит уже в готовой яркости/контрасте. Инвертирование добавим
    // при необходимости отдельным инструментом.
    windowPresets: [],
  },
  // Пункты систематического осмотра. reviewedChecklist попытки сверяется
  // с этими ключами — полнота осмотра и есть доля пройденных пунктов.
  checklist: [
    { key: "airway", label: "A — дыхательные пути: трахея по средней линии, бронхи" },
    { key: "breathing", label: "B — лёгочные поля: прозрачность, очаги, симметрия" },
    { key: "circulation", label: "C — сердце и средостение: размеры, контуры" },
    { key: "diaphragm", label: "D — диафрагма и рёберно-диафрагмальные синусы" },
    { key: "everything", label: "E — кости и мягкие ткани: рёбра, ключицы, ткани" },
    { key: "devices", label: "Трубки/катетеры/инородные тела (если есть)" },
  ],
  scoring: {
    // Для обзорного снимка локализация и диагноз — главное; вес чуть
    // смещён на них относительно умолчаний.
    weights: {
      detection: 0.35,
      classification: 0.15,
      checklist: 0.15,
      diagnosis: 0.25,
      aiImpression: 0.1,
    },
    matchRadius: 0.1,
    falseAlarmPenalty: 1,
    passThreshold: 0.7,
  },
});
