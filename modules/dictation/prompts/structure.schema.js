// server/modules/dictation/prompts/structure.schema.js
//
// Схема черновика осмотра и правила его сборки.
//
// ГЛАВНЫЙ ПРИНЦИП: МОДЕЛЬ — ПИСАРЬ, А НЕ ВРАЧ. В карту попадает только то,
// что врач произнёс. Модель не додумывает, не «дополняет по клинической
// картине» и не подставляет типичное. Не сказано про аллергоанамнез — поле
// пустое, а не «без особенностей».
//
// Технически это выражено двумя решениями, и оба обязательны:
//   1. КАЖДОЕ поле допускает null. Пустой черновик — корректный результат.
//   2. В системном промпте прямой запрет на значения по умолчанию.
//
// Без первого модель вынуждена что-то написать в обязательное поле — и
// напишет правдоподобное. Это худший вид дефекта: выдумка, неотличимая от
// записи врача.
//
// Имена полей повторяют newPatientMedicalHistory (statusPreasens,
// statusLocalis, recommendations) намеренно: чем меньше переименований между
// схемой и картой, тем меньше места для ошибки маппинга.

/** Строка или null — базовый тип для всех текстовых полей черновика. */
const nullableString = (description) => ({
  type: ["string", "null"],
  description,
});

export const STRUCTURE_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: [
    "complaints",
    "anamnesisMorbi",
    "anamnesisVitae",
    "statusPreasens",
    "statusLocalis",
    "mainDiagnosisText",
    "mainDiagnosisCode",
    "recommendations",
    "ctScanResults",
    "mriResults",
    "ultrasoundResults",
    "laboratoryTestResults",
  ],
  properties: {
    complaints: nullableString(
      "Жалобы пациента, как их изложил врач. null — если врач о жалобах не говорил.",
    ),
    anamnesisMorbi: nullableString(
      "Анамнез заболевания: когда началось, как развивалось, чем лечились. null — если не прозвучало.",
    ),
    anamnesisVitae: nullableString(
      "Анамнез жизни: перенесённые заболевания, операции, аллергии, наследственность, вредные привычки. null — если не прозвучало.",
    ),
    statusPreasens: nullableString(
      "Объективный статус: общее состояние, данные осмотра по системам. null — если врач осмотр не описывал.",
    ),
    statusLocalis: nullableString(
      "Локальный статус: состояние поражённой области. null — если не прозвучало.",
    ),
    mainDiagnosisText: nullableString(
      "Формулировка основного диагноза словами врача. null — если диагноз не назван.",
    ),
    mainDiagnosisCode: nullableString(
      "Код МКБ-10, ТОЛЬКО если врач назвал его вслух или формулировка соответствует коду однозначно. При малейшем сомнении — null. Ошибочный код хуже отсутствующего: он уходит в статистику и в счета.",
    ),
    recommendations: nullableString(
      "План: назначения, рекомендации, сроки контроля. null — если не прозвучало.",
    ),
    // Результаты исследований разложены по видам, а не свалены в одно поле:
    // ровно четыре поля есть в карте, и разбирать общий текст пришлось бы
    // приёмнику — то есть догадкой. Пусть классифицирует тот, кто слышал
    // фразу целиком.
    ctScanResults: nullableString(
      "Результаты КТ, если врач их проговорил. null — если не прозвучало.",
    ),
    mriResults: nullableString(
      "Результаты МРТ, если врач их проговорил. null — если не прозвучало.",
    ),
    ultrasoundResults: nullableString(
      "Результаты УЗИ, если врач их проговорил. null — если не прозвучало.",
    ),
    laboratoryTestResults: nullableString(
      "Результаты лабораторных исследований, если врач их проговорил. null — если не прозвучало.",
    ),
  },
};

// Системный промпт. Запреты сформулированы конкретно, а не общим призывом
// «не выдумывай»: калибровка на медицинском материале показывает, что общая
// формулировка не мешает модели вписать «состояние удовлетворительное» в
// пустое поле, а перечисление конкретных запрещённых заполнителей — мешает.
export const STRUCTURE_SYSTEM = `Ты — медицинский писарь. Врач надиктовал осмотр пациента своими словами; твоя задача — разложить сказанное по полям истории болезни. Ты НЕ ставишь диагноз, НЕ назначаешь лечение и НЕ дополняешь картину.

Жёсткие правила:
1. Используй ТОЛЬКО то, что врач произнёс. Ничего не добавляй от себя.
2. Если о чём-то не сказано — поле равно null. Запрещено подставлять «без особенностей», «в норме», «не отягощён», «состояние удовлетворительное», «жалоб нет» и любые другие заполнители. Пустое поле — правильный ответ.
3. Разговорную речь переводи в медицинскую, НЕ меняя смысла и не добавляя деталей. «Живот мягкий, не болит» → «Живот мягкий, безболезненный». Но «болит голова» НЕ превращается в «цефалгия неуточнённая».
4. Код МКБ-10 ставь, только если врач назвал его вслух или формулировка соответствует коду однозначно. Сомневаешься — null, а диагноз оставь текстом.
5. Не переставляй сведения между полями по своему усмотрению: жалобы — в жалобы, осмотр — в объективный статус.
6. Числа, дозы, единицы измерения и сроки переноси дословно. Если расслышано неуверенно — оставь как есть, не «исправляй» на правдоподобное.
7. Пиши на языке надиктовки.

Ответ верни строго в заданной JSON-структуре.`;
